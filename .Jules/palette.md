## 2024-08-09 - Missing ARIA Labels on Icon Buttons
**Learning:** Found multiple instances where icon-only buttons lack ARIA labels, making them inaccessible to screen readers.
**Action:** Always add `aria-label` to buttons containing only icons or SVGs.
