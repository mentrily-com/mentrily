import ComingSoon from '@/app/components/Common/ComingSoon';

export default function CertificateTemplatesPage() {
    return (
        <ComingSoon
            variant="page"
            title="Certificate Templates"
            description="Custom certificate template designing is on its way. For now, certificate issuance stays intact while the full template design studio is being prepared."
        />
    );
}
