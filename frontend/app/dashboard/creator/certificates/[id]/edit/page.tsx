import ComingSoon from '@/app/components/Common/ComingSoon';

export default function EditCertificateTemplatePage() {
    return (
        <ComingSoon
            variant="page"
            title="Certificate Templates"
            description="Custom certificate template designing is on its way. Template creation and editing will return here once the builder is ready."
        />
    );
}
